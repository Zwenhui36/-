<view class="view">
  <image class="answerimage" src="/images/doctor/1.png"></image>
  <view class="container" bindtap="gowebView" data-url="{{url}}">
    <view class="top">{{title}}</view>
    <view class="bottom">
      <view class="description">{{description}}</view>
      <image src="{{picurl}}" class="image"></image>
    </view>
  </view>
</view>